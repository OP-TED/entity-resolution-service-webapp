services:
  app:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile
      args:
        ENVIRONMENT: development
    ports:
      - "8080:80"
    environment:
      - VITE_APP_MAIN_API=${VITE_APP_MAIN_API}
